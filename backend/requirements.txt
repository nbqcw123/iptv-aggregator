# ============================================================
# IPTV 聚合器 - Python 依赖
# ============================================================
# 核心框架
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# 异步 HTTP 客户端（抓取 IPTV 源）
aiohttp>=3.9.0

# 数据库 ORM
sqlalchemy>=2.0.0

# 文件上传支持
python-multipart>=0.0.9

# 定时任务（自动更新频道列表）
apscheduler>=3.10.0
